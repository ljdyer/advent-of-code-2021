import math


def MONAD(input_: str):

    input_ = list(input_)
    x = 0
    y = 0
    z = 0
    w = 0

    w = int(input_.pop(0))
    # x = x * 0
    # x = x + z
    # x = x % 26
    # z = math.floor(z / 1)
    # x = 12
    # x = 0
    x = 1
    # y = 25
    # y = 0
    # y = y * x
    # y = 1
    # z = z * y
    y = 0
    # y = w
    y = w + 6
    # y = y * x
    z = y
    
    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 1)
    x = x + 10
    x = int(x==w)
    x = int(x==0)
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 6
    y = y * x
    z = z + y
    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 1)
    x = x + 13
    x = int(x==w)
    x = int(x==0)
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 3
    y = y * x
    z = z + y
    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 26)
    x = x + -11
    x = int(x==w)
    x = int(x==0)
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 11
    y = y * x
    z = z + y
    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 1)
    x = x + 13
    x = int(x==w)
    x = int(x==0)
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 9
    y = y * x
    z = z + y
    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 26)
    x = x + -1
    x = int(x==w)
    x = int(x==0)
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 3
    y = y * x
    z = z + y
    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 1)
    x = x + 10
    x = int(x==w)
    x = int(x==0)
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 13
    y = y * x
    z = z + y
    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 1)
    x = x + 11
    x = int(x==w)
    x = int(x==0)
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 6
    y = y * x
    z = z + y
    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 26)
    x = x + 0
    x = int(x==w)
    x = int(x==0)
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 14
    y = y * x
    z = z + y
    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 1)
    x = x + 10
    x = int(x==w)
    x = int(x==0)
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 10
    y = y * x
    z = z + y
    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 26)
    x = x + -5
    x = int(x==w)
    x = int(x==0)
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 12
    y = y * x
    z = z + y
    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 26)
    x = x + -16
    x = int(x==w)
    x = int(x==0)
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 10
    y = y * x
    z = z + y
    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 26)
    x = x + -7
    x = int(x==w)
    x = int(x==0)
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 11
    y = y * x
    z = z + y
    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 26)
    x = x + -11
    x = int(x==w)
    x = int(x==0)
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 15
    y = y * x
    z = z + y

    if z == 0:
        return True
    else:
        return False



inputs = (str(x) for x in range(99999999999999,11111111111111,-1) if not '0' in str(x))

for i in inputs:
    if MONAD(i):
        print(i)
        quit()
