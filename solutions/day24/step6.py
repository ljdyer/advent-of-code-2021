import math


def MONAD1(input_: str):

    input_ = list(input_)
    x = 0
    y = 0
    z = 0
    w = 0

    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 1)
    x = x + 12
    x = int(x==w)
    x = int(x==0)
    y = y + 25
    y = y * 0
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 6
    y = y * x
    z = z + y

    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 1)
    x = x + 10
    x = int(x==w)
    x = int(x==0)
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 6
    y = y * x
    z = z + y

    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 1)
    x = x + 13
    x = int(x==w)
    x = int(x==0)
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 3
    y = y * x
    z = z + y

    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 26)
    x = x + -11
    x = int(x==w)
    x = int(x==0)
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 11
    y = y * x
    z = z + y

    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 1)
    x = x + 13
    x = int(x==w)
    x = int(x==0)
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 9
    y = y * x
    z = z + y

    w = int(input_.pop(0))
    x = x * 0
    x = x + z
    x = x % 26
    z = math.floor(z / 26)
    x = x + -1
    x = int(x==w)
    x = int(x==0)
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 3
    y = y * x
    z = z + y
    # w = int(input_.pop(0))
    # x = x * 0
    # x = x + z
    # x = x % 26
    # z = math.floor(z / 1)
    # x = x + 10
    # x = int(x==w)
    # x = int(x==0)
    # y = y * 0
    # y = y + 25
    # y = y * x
    # y = y + 1
    # z = z * y
    # y = y * 0
    # y = y + w
    # y = y + 13
    # y = y * x
    # z = z + y
    # w = int(input_.pop(0))
    # x = x * 0
    # x = x + z
    # x = x % 26
    # z = math.floor(z / 1)
    # x = x + 11
    # x = int(x==w)
    # x = int(x==0)
    # y = y * 0
    # y = y + 25
    # y = y * x
    # y = y + 1
    # z = z * y
    # y = y * 0
    # y = y + w
    # y = y + 6
    # y = y * x
    # z = z + y
    # w = int(input_.pop(0))
    # x = x * 0
    # x = x + z
    # x = x % 26
    # z = math.floor(z / 26)
    # x = x + 0
    # x = int(x==w)
    # x = int(x==0)
    # y = y * 0
    # y = y + 25
    # y = y * x
    # y = y + 1
    # z = z * y
    # y = y * 0
    # y = y + w
    # y = y + 14
    # y = y * x
    # z = z + y
    # w = int(input_.pop(0))
    # x = x * 0
    # x = x + z
    # x = x % 26
    # z = math.floor(z / 1)
    # x = x + 10
    # x = int(x==w)
    # x = int(x==0)
    # y = y * 0
    # y = y + 25
    # y = y * x
    # y = y + 1
    # z = z * y
    # y = y * 0
    # y = y + w
    # y = y + 10
    # y = y * x
    # z = z + y
    # w = int(input_.pop(0))
    # x = x * 0
    # x = x + z
    # x = x % 26
    # z = math.floor(z / 26)
    # x = x + -5
    # x = int(x==w)
    # x = int(x==0)
    # y = y * 0
    # y = y + 25
    # y = y * x
    # y = y + 1
    # z = z * y
    # y = y * 0
    # y = y + w
    # y = y + 12
    # y = y * x
    # z = z + y
    # w = int(input_.pop(0))
    # x = x * 0
    # x = x + z
    # x = x % 26
    # z = math.floor(z / 26)
    # x = x + -16
    # x = int(x==w)
    # x = int(x==0)
    # y = y * 0
    # y = y + 25
    # y = y * x
    # y = y + 1
    # z = z * y
    # y = y * 0
    # y = y + w
    # y = y + 10
    # y = y * x
    # z = z + y
    # w = int(input_.pop(0))
    # x = x * 0
    # x = x + z
    # x = x % 26
    # z = math.floor(z / 26)
    # x = x + -7
    # x = int(x==w)
    # x = int(x==0)
    # y = y * 0
    # y = y + 25
    # y = y * x
    # y = y + 1
    # z = z * y
    # y = y * 0
    # y = y + w
    # y = y + 11
    # y = y * x
    # z = z + y
    # w = int(input_.pop(0))
    # x = x * 0
    # x = x + z
    # x = x % 26
    # z = math.floor(z / 26)
    # x = x + -11
    # x = int(x==w)
    # x = int(x==0)
    # y = y * 0
    # y = y + 25
    # y = y * x
    # y = y + 1
    # z = z * y
    # y = y * 0
    # y = y + w
    # y = y + 15
    # y = y * x
    # z = z + y

    # if z == 0:
    #     return True
    # else:
    #     return False

    return {'x': x, 'y': y, 'z': z, 'w': w}


def MONAD2(input_: str):

    input_ = list(input_)
    x = 0
    y = 0
    z = 0
    w = 0

    w = int(input_.pop(0))
    x = 1
    z = w + 6
    # print( {'x': x, 'y': y, 'z': z, 'w': w})

    w = int(input_.pop(0))
    x = 1
    z = (z * 26) + w + 6
    # print( {'x': x, 'y': y, 'z': z, 'w': w})
    
    w = int(input_.pop(0))
    x = 1
    z = (z * 26) + (w + 3)
    # print( {'x': x, 'y': y, 'z': z, 'w': w})
    
    w = int(input_.pop(0))
    x = int((z % 26 - 11) != w) # w = 1
    z = (z // 26) * (25 * x + 1) + ((w + 11) * x)

    w = int(input_.pop(0))
    x = 1
    z = (z * 26) + (w + 9)
    # print( {'x': x, 'y': y, 'z': z, 'w': w})
    
    w = int(input_.pop(0))
    x = (z % 26) - 1
    z = z // 26
    x = int(x==w)
    x = int(x==0)
    y = (25 * x + 1)
    z = z * y
    y = (w + 3) * x
    z = z + y
    # print( {'x': x, 'y': y, 'z': z, 'w': w})


    # w = int(input_.pop(0))
    # x = x * 0
    # x = x + z
    # x = x % 26
    # z = math.floor(z / 1)
    # x = x + 10
    # x = int(x==w)
    # x = int(x==0)
    # y = y * 0
    # y = y + 25
    # y = y * x
    # y = y + 1
    # z = z * y
    # y = y * 0
    # y = y + w
    # y = y + 13
    # y = y * x
    # z = z + y
    # w = int(input_.pop(0))
    # x = x * 0
    # x = x + z
    # x = x % 26
    # z = math.floor(z / 1)
    # x = x + 11
    # x = int(x==w)
    # x = int(x==0)
    # y = y * 0
    # y = y + 25
    # y = y * x
    # y = y + 1
    # z = z * y
    # y = y * 0
    # y = y + w
    # y = y + 6
    # y = y * x
    # z = z + y
    # w = int(input_.pop(0))
    # x = x * 0
    # x = x + z
    # x = x % 26
    # z = math.floor(z / 26)
    # x = x + 0
    # x = int(x==w)
    # x = int(x==0)
    # y = y * 0
    # y = y + 25
    # y = y * x
    # y = y + 1
    # z = z * y
    # y = y * 0
    # y = y + w
    # y = y + 14
    # y = y * x
    # z = z + y
    # w = int(input_.pop(0))
    # x = x * 0
    # x = x + z
    # x = x % 26
    # z = math.floor(z / 1)
    # x = x + 10
    # x = int(x==w)
    # x = int(x==0)
    # y = y * 0
    # y = y + 25
    # y = y * x
    # y = y + 1
    # z = z * y
    # y = y * 0
    # y = y + w
    # y = y + 10
    # y = y * x
    # z = z + y
    # w = int(input_.pop(0))
    # x = x * 0
    # x = x + z
    # x = x % 26
    # z = math.floor(z / 26)
    # x = x + -5
    # x = int(x==w)
    # x = int(x==0)
    # y = y * 0
    # y = y + 25
    # y = y * x
    # y = y + 1
    # z = z * y
    # y = y * 0
    # y = y + w
    # y = y + 12
    # y = y * x
    # z = z + y
    # w = int(input_.pop(0))
    # x = x * 0
    # x = x + z
    # x = x % 26
    # z = math.floor(z / 26)
    # x = x + -16
    # x = int(x==w)
    # x = int(x==0)
    # y = y * 0
    # y = y + 25
    # y = y * x
    # y = y + 1
    # z = z * y
    # y = y * 0
    # y = y + w
    # y = y + 10
    # y = y * x
    # z = z + y
    # w = int(input_.pop(0))
    # x = x * 0
    # x = x + z
    # x = x % 26
    # z = math.floor(z / 26)
    # x = x + -7
    # x = int(x==w)
    # x = int(x==0)
    # y = y * 0
    # y = y + 25
    # y = y * x
    # y = y + 1
    # z = z * y
    # y = y * 0
    # y = y + w
    # y = y + 11
    # y = y * x
    # z = z + y

    # w = int(input_.pop(0))
    # x = x * 0
    # x = x + z
    # x = x % 26
    # z = math.floor(z / 26)
    # x = x + -11
    # x = int(x!=w)
    # y = 25 * x + 1
    # z = z * y
    # y = (w + 15) * x
    # z = z + y

    # if z == 0:
    #     return True
    # else:
    #     return False

    return {'x': x, 'y': y, 'z': z, 'w': w}


for a in range(111111,999999):
    if not '0' in str(a):
        assert MONAD1(str(a)) == MONAD2(str(a))
print('passed')

# MONAD2('222222')
